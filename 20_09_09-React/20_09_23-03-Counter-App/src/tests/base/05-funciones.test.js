import '@testing-library/jest-dom';

import { getUser, getUsuarioActivo } from '../../base/05-funciones'

describe('Pruebas en 05-funciones', () => {

    // test('getUser debe de retornar un objeto ', () => {

    //     const userTest = {
    //     uid: 'ABD123',
    //     username: 'El_Papi1502'
    //     }

    //     const user = getUser();

    //     expect(user).toEqual(userTest);
    // })

// getUsuarioActio debe de retornar un objeto
// getusuario activo debe retornar un objeto

// evaloar lo que se retorna en "username:nobre", con un objeto que vamos a definir en la otra pagina, getuseractivo debe retornar un nombre en el username:

// test('getUser debe de retornar el nombre ', () => {

//     uid: 'ABD123',
//     username: (nombre);
//     }

//     const user = getUser(nombre);

//     expect(user).toEqual(userTest);
// })

/**  */
/**  */
// RESPUESTA
test('getUser debe de retornar un objeto  ', () => {
    
    const nombre = 'Juan',
    const user = getUsuarioActivo(nombre)

    console.log(object);
})
})
/**  */
/**  */
